services:
  mcp-mixsearch:
    build:
      context: ../..
      dockerfile: Dockerfile
    volumes:
      - ../..:/app
    working_dir: /app
    command: ["--from", ".", "mcp-mixsearch", "--mode", "mcp"]
    ports:
      - "3000:3000"
    environment:
      - UV_CACHE_DIR=/tmp/uv-cache
      - MCP_ONLY=true
    restart: unless-stopped